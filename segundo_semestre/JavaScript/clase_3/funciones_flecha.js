// Funciones flecha
// Son una forma más concisa de escribir funciones en JavaScript.
// No tienen su propio "this", "arguments" o "super".

const sumar = (a, b) => a + b;

resultado = sumar(3, 7);
console.log(resultado);